
machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  tiemzone:
    America/Los_Angeles
  node:
    version: 7.1.0

dependencies:
  override:
    - yarn

test:
  override:
    - yarn test

deployment:
  staging: 
    branch: development
    commands:
      - yarn staging
  production: 
    branch: development
    commands:
      - yarn production
